on:
  push:
    branches:
      - master
      
jobs:
  build:    
    runs-on: ubuntu-latest
    steps:
      - name: build1
        uses: actions/checkout@v2                       
      - run: npm i
      - run: npm run build
